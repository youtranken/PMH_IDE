# deps
node_modules/
.pnpm-store/

# build
dist/
build/
*.tsbuildinfo

# env & secrets — NEVER commit (AD-15)
.env
*.env.local
deploy/nginx/certs/*.pem
deploy/nginx/certs/*.key
deploy/nginx/certs/*.crt
# nguồn cert/PFX gốc (private key + mật khẩu PFX) — tuyệt đối không commit
pmh.com.vn/
*.pfx
*.key

# signing keys / KEK (AD-6/AD-8/AD-15) — kept on mounted volume, out of git
secrets/
keys/

# test artifacts (playwright / ảnh chụp verify)
.playwright-mcp/
/login-via-edge.png

# graphify: bản đồ code sinh tự động (5MB+, đổi mỗi lần sửa code). Dựng lại bằng
# `graphify update .` (chỉ AST, không tốn API) — không commit để khỏi phình lịch
# sử + xung đột merge trên JSON to.
graphify-out/

# logs / misc
*.log
.DS_Store
Thumbs.db

# backup .env tu script sinh secret (chua secret)
.env.bak.*
