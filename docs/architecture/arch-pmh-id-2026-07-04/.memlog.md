# memlog — Architecture Spine PMH ID
scope: Hệ thống SSO/IdP nội bộ PMH ID (~1000 user, 3-5 project, on-premise + public internet)
purpose: Spine kiến trúc làm đầu vào cho epics/stories; chốt các điểm PRD để lại cho architecture
altitude: initiative → features

- 2026-07-04 event: bắt đầu run create; input = prd.md (final) + addendum.md tại docs/prds/prd-sso-pmh-2026-07-04
- 2026-07-04 constraint: PRD đã final — kế thừa các quyết định, không re-decide (tự quản mật khẩu, OIDC verify offline, client_groups là nguồn quyền, 2 SSA + project_admin, soft-delete, MFA SSA, idle bám session)
- 2026-07-04 note: memlog.py không khả dụng (cài qua plugin) — ghi tay cùng định dạng
- 2026-07-04 direction: các điểm PRD cố ý để architecture chốt = chống dò mật khẩu, rotate khóa ký JWT (ưu tiên built-in node-oidc-provider), idle-timeout bám session, rotate client_secret ân hạn, webhook egress allowlist, topology deploy
- 2026-07-04 decision: paradigm = modular monolith (1 app NestJS chia module). Binds: mọi tính năng là module trong 1 process. Prevents: phân mảnh microservice quá mức cho 1000 user/2 người vận hành
- 2026-07-04 decision: repo = monorepo (sso-server NestJS, portal-fe React/AntD, demo-app, shared contract/types). Prevents: lệch hợp đồng API giữa FE-BE và giữa các project
- 2026-07-04 decision: SSO portal tách FE/BE chung domain id.pmh.com.vn — SPA React tĩnh; NestJS = /oidc + /api. Prevents: trộn build FE vào BE
- 2026-07-04 decision: reverse proxy = Nginx trong Docker Compose, TLS termination + route. Binds: mọi service sau Nginx; app phải set provider.proxy=true, sanitize X-Forwarded-* tại Nginx
- 2026-07-04 version: thư viện OIDC = `oidc-provider` v9.x (npm, panva), ESM-only, Node ^20.19/^22.12/>=23. Package KHÔNG phải 'node-oidc-provider'
- 2026-07-04 constraint: oidc-provider v9 ESM-only, NestJS CJS → cần interop dynamic import() khi nhúng provider
- 2026-07-04 constraint: KHÔNG auto-rotate khóa ký — tự quản mảng jwks.keys, quy trình 2 bước (thêm cuối→reload→đưa lên đầu→giữ khóa cũ verify), kid tự sinh
- 2026-07-04 constraint: session KHÔNG có sẵn idle+absolute tách biệt — tự lưu authTime/last-activity, tự tính trong ttl.Session (idle 15' + cap 12h)
- 2026-07-04 constraint: KHÔNG có adapter Postgres chính thức — tự viết Adapter (upsert/find/findByUid/findByUserCode/consume/destroy/revokeByGrantId)
- 2026-07-04 assumption: rủi ro bus-factor kép — oidc-provider gần như 1 maintainer + 2 SSA; cần pin version, theo dõi CVE
- 2026-07-04 decision: Directory API M2M dùng client_secret (client_secret_basic/post) — ưu tiên dễ tích hợp cho dev ngoài; secret hash + rotate. Prevents: rào cản tích hợp làm hỏng mục tiêu G4
- 2026-07-04 decision: chống dò mật khẩu đặt trong app NestJS (guard + login_attempts): backoff theo account + đếm theo IP + SSA miễn khóa. Prevents: khóa cứng SSA từ ngoài; giữ logic 'theo email' mà Nginx không làm được
- 2026-07-04 decision: khóa ký JWT lưu file mount (ngoài image/git), quyền đọc hạn chế, backup riêng mỗi lần rotate. Prevents: vòng luẩn quẩn master-key-trong-DB
- 2026-07-04 decision: chặn webhook SSRF trong app — validate khi lưu (resolve DNS, chặn dải private, allowlist CIDR) + connect IP đã pin khi gửi. Prevents: SSRF tới metadata/nội bộ + DNS rebinding
- 2026-07-04 decision [ADOPTED]: login/consent UI là route trong React SPA; oidc-provider redirect tới /interaction/:uid do app tự render (interactions pattern chuẩn của thư viện)
- 2026-07-04 decision: xử lý nền = @nestjs/schedule (cron: auto-lock expires_at, nén audit tháng) + hàng đợi bằng bảng Postgres (email, webhook retry) với worker in-process. KHÔNG Redis. Binds: mọi tác vụ async qua job/queue trong Postgres; interface queue tách biệt để đổi ruột sau nếu cần. Prevents: thêm hạ tầng thừa cho quy mô 1000 user
- 2026-07-04 assumption: nút cổ chai email là Gmail SMTP rate limit, không phải queue backend — worker gửi có throttle
- 2026-07-04 decision: cấu hình 2 tầng — .env giữ bí mật hạ tầng (DB creds, cookie keys, SMTP creds, đường dẫn khóa ký); bảng Settings giữ tham số SSA chỉnh runtime (TTL, policy MK, hạn MK tạm, path backup, tham số chống dò). Prevents: bí mật lọt vào DB + lẫn cấu hình runtime với secrets
- 2026-07-04 decision: app demo bàn giao = Node/Express + openid-client, thể hiện login OIDC + verify JWT offline + gọi Directory API + nhận webhook. Deliverable kèm docs tích hợp (FR-33/34)
- 2026-07-04 event: hoàn tất coaching; distill spine 16 AD + seed + deferred
- 2026-07-04 event: reviewer gate = tái triệu Code Review Crew (Vex/Grumbal/Boundary/Dana/Yui) soi spine; xung đột chính Vex/Grumbal (gia cố) vs Dana (đang tự viết nửa Keycloak, hoãn mạ vàng); Boundary trục "vỡ khi lỡ scale >1 instance"; Yui bắt mâu thuẫn spine↔addendum
- 2026-07-04 change: áp 12 sửa 🔴 vào spine + addendum:
  - AD-3: /docs gate group Developers ở BE
  - AD-5: pin Node patch + kế hoạch CVE/fork (1-maintainer)
  - AD-6: consume() atomic + revokeByGrantId transactional + map grant→client→project cho thu hồi phân thẩm quyền FR-05; khóa ký KHÔNG trong DB
  - AD-7: authTime null → fail-closed, UTC
  - AD-8: publish-trước-ký-sau; overlap ≥ access TTL + JWKS cache; backup khóa mã hóa cùng nhịp DB
  - AD-9: phủ /oidc/token (brute secret); backoff-account trước, IP theo metric
  - AD-11: allow_all_groups KHÔNG nới scope Directory; tách login-access vs directory-read; rate-limit /users
  - AD-13: SELECT FOR UPDATE SKIP LOCKED; heartbeat + cảnh báo job pending-quá-tuổi
  - AD-15: KEK TOTP tách khỏi ciphertext; cookie keys vào .env
  - AD-16: backup trọn bộ (DB+.env+cookie keys+KEK+khóa ký) cùng nhịp mã hóa; runbook dựng lại host trắng + diễn tập break-glass
- 2026-07-04 decision [Deferred]: webhook pin-IP + chống DNS-rebinding hoãn (Dana: mạ vàng, webhook tùy chọn chưa ai dùng) — giữ https+CIDR+HMAC; kèm re-pin DHCP + cảnh báo secret hết ân hạn
- 2026-07-04 change: sửa mâu thuẫn addendum — tên oidc-provider v9 (bỏ node-oidc-provider), tự viết Adapter (bỏ 'kèm adapter'), thêm user.deleted vào webhook events, khóa ký không trong oidc_payloads
- 2026-07-04 event: spine finalized
- 2026-07-04 event: rà soát khớp PRD↔spine↔addendum (2 agent độc lập) — khớp tốt; vá 15 gap (4 Cao, 7 TB, 4 Thấp)
- 2026-07-04 change: AD-7 refresh token trói Session + sessions là projection (oidc_payloads là nguồn sự thật); auth_time thay authTime
- 2026-07-04 change: AD-8 mandate max JWKS cache client = 10' trong hợp đồng shared
- 2026-07-04 change: AD-3 chốt rõ /interaction = Nginx trả SPA index.html, SPA gọi API /api/interaction (BE cấp API, FE render)
- 2026-07-04 change: AD-13 thêm email_queue + cleanup cron oidc_payloads + cột claim locked_at; AD-16 kênh cảnh báo out-of-band (Telegram/Zalo ngoài container)
- 2026-07-04 change: AD-1 gắn audit_logs.project_id cho scope FR-30/FR-14; AD-14 webhook_secret_enc bọc KEK
- 2026-07-04 change: schema addendum bổ sung cột load-bearing (sessions.auth_time+oidc_session_uid, oidc_payloads cột đầy đủ, login_attempts.client_id, users.is_breakglass, audit_logs.project_id, email_queue, client_secrets.revoked, locked_at); bỏ otp_codes (vết tích OTP email)
- 2026-07-04 event: spine re-finalized sau vá gaps
- 2026-07-04 event: sinh backlog docs/backlog/ — 9 epic, 51 story; đối chiếu phủ đủ 34/34 FR + 16/16 AD, không phụ thuộc gãy
- 2026-07-04 change: vá 4 điểm sau đối chiếu — dời Quên-mật-khẩu E2-S5→E4-S8 (gỡ cross-phase); thêm E1-S8 nạp claims JWT (FR-02); nối đường thu hồi FR-05 (E4-S2/E5-S2/E6-S2)
- 2026-07-04 decision: sơ đồ cổng host tránh project `app` (đang chiếm 8080/8443) — Nginx 9443:443 + 9080:80, Postgres 5433:5432 (dev only), Mailpit 8026:8025 + 1026:1025, NestJS/worker không publish; cổng qua .env (HOST_*_PORT). Binds: E0-S2 compose. Prevents: đụng cổng project đang chạy
- 2026-07-04 event: hiện thực Epic 0 — monorepo pnpm (apps/sso-server NestJS, portal-fe React+Vite+AntD, demo-app Express, packages/shared), Docker Compose (Postgres16/Mailpit/Nginx TLS/sso-server/portal-fe), migration base schema đủ 19 bảng + seed settings, cấu hình 2 tầng (.env validate fail-fast + SettingsService cache). Build 4 package sạch (tsc/nest build exit 0)
- 2026-07-04 decision: stack cụ thể — pnpm workspaces, Node 22-alpine container, node-postgres Pool (không ORM, để tự viết OIDC adapter AD-6), node-pg-migrate (-j ts) chạy lúc container start, Vite dev server sau Nginx (HMR wss qua cổng public), cert self-signed qua deploy/gen-certs.sh
- 2026-07-04 event: Epic 0 VERIFY chạy thật — docker compose up 5 service healthy; migration áp 19 bảng + 12 settings seed; GET https://localhost:9443/api/health => {status:ok,db:up,claimsVersion:1}; HTTP 9080→301 HTTPS; portal HTML 200 qua Nginx TLS. Chuỗi FE→Nginx→NestJS→Postgres thông
- 2026-07-04 change: 3 fix lúc dựng — (1) rootDir=src cho sso-server (migrations trong include làm dist lồng dist/src, mất dist/main.js); (2) compose cần --env-file .env (project-dir mặc định = deploy/, không nội suy được ${POSTGRES_*}); (3) Nginx đặt proxy_set_header ở server-level + map $connection_upgrade (location / khai báo header riêng làm mất kế thừa Host → Vite chặn host portal_fe)
- 2026-07-04 event: thêm CLAUDE.md (copy từ E:/PMH/DE) làm quy tắc code — Karpathy guidelines + BMad coordination + tool routing
- 2026-07-04 event: Epic 1 HOÀN TẤT 9/9 story, verify chạy thật — ESM interop qua new Function('return import()'); PgAdapter 8/8 test (consume atomic, replay→revoke trọn grant); login interaction SPA+API với lỗi đồng nhất; TTL idle+cap fail-closed từ Settings (test co idle=10s: refresh quá idle bị từ chối, refresh không reset idle); rotateRefreshToken + expiresWithSession (refresh trói phiên); logout + sessions.revoked_at; claims 6 trường phản ánh group live; demo-app openid-client v6 + jose verify offline e2e
- 2026-07-04 decision: access token = JWT RS256 verify-offline (hợp đồng docs) qua features.resourceIndicators — defaultResource urn:pmh:api, aud=client_id, extraTokenClaims nhét claims hợp đồng. Binds: mọi client verify offline không cần userinfo. Prevents: app gọi về IdP mỗi request
- 2026-07-04 decision: bỏ màn consent (first-party nội bộ) — loadExistingGrant tự cấp Grant đúng scope + resource scope PHẢI khớp getResourceServerInfo.scope (lệch là provider bật consent prompt đòi cấp thêm — bug đã gặp và fix)
- 2026-07-04 decision: client demo dev = static config từ env (chỉ khi NODE_ENV != production); client thật từ DB đến ở E5-S5 — lưu ý oidc-provider so client_secret PLAINTEXT trong metadata, E5 phải giải bài toán secret hash (nghiên cứu riêng)
- 2026-07-04 constraint: cookie interaction của oidc-provider mặc định Path=/interaction/:uid — phải nới cookies.short.path='/' để cookie đến được /api/interaction (AD-3)
- 2026-07-04 constraint: Nginx phải dùng $http_host (không phải $host — cắt mất port) cho Host/X-Forwarded-Host, không thì URL trong discovery thiếu :9443
- 2026-07-04 change: fix form login không hiện — @pmh/shared là workspace CJS, Vite không pre-bundle linked package → browser import named 'JWT_CLAIMS_VERSION' crash → React không render (root rỗng). Fix: vite optimizeDeps.include=['@pmh/shared'] + commonjsOptions. Xác nhận bằng headless Chrome DOM dump (form render OK)
- 2026-07-04 decision: thêm dev:fast (docker-compose.dev.yml) — mount source + watch, KHÔNG rebuild image khi sửa code. sso-server: MỘT chủ dist = tsc -w + node --watch chờ dist/main.js (nest build/deleteOutDir tranh xóa gây restart loop — đã bỏ). portal-fe: Vite HMR qua mount. Chỉ rebuild image (pnpm dev) khi đổi package.json. Verify: sửa src backend → reload 9s
- 2026-07-04 event: code review adversarial (party Code Review Crew, 4 subagent đọc code thật + đọc source oidc-provider@9.8.6) trên Epic 0+1. Boundary BÁC nghi vấn Critical ttl.Session/loginTs (báo động giả — loginTs do lib set, E1-S5/S6 pass thật). Ra 7 mục sửa-ngay + nợ đúng epic (client_groups gate→E5, rate-limit→E2, projection zombie→E6)
- 2026-07-05 change: vá 7 mục sau review, verify từng cái: (1) Pool connectionTimeoutMillis 5s + statement_timeout 10s + max 20; /api/health ping deadline 2s trả 503 khi down (không treo theo pool cạn). (2) TTL đọc live qua settings.getIntSync trong closure + SettingsService.preload + set() cập nhật cache in-place (SSA đổi runtime có hiệu lực ngay, không restart). (3) env.validation fail-fast prod nếu COOKIE_KEYS/KEK_BASE64 trùng mẫu .env.example. (4) healthcheck sso-server (node http /api/health) + nginx depends_on condition service_healthy. (5) demo-app esc() HTML mọi claim (chống XSS mẫu copy). (6) consume() bọc BEGIN/COMMIT + SELECT FOR UPDATE (đóng cửa sổ SELECT-then-DELETE, AD-6). (7) @pmh/shared PMH_CLAIM_KEYS nguồn sự thật + _AssertKeysMatch chốt biên dịch + loadUserClaims: Promise<PmhIdTokenClaims> (chống lệch FR-02)
- 2026-07-05 constraint: đọc DB trực tiếp KHÔNG cập nhật cache SettingsService — chỉ settings.set() mới ghi cache in-place. E6-S5 phải gọi set(), không UPDATE thẳng SQL
- 2026-07-05 event: Epic 2 HOÀN TẤT 4/4 story, verify thật (14/15 assert, 1 assert sai kỳ vọng — backoff chặn trước khi ghi login_attempts nên count dừng ở threshold, đúng). E2-S4 policy (checklist @pmh/shared, bắt đổi khi must_change/hết hạn 90d), E2-S1 MFA TOTP (otplib, secret AES-256-GCM KEK, enroll QR 2 bước), E2-S2 recovery 10 code + break-glass bỏ qua MFA + audit nổi bật, E2-S3 backoff account + phủ /oidc/token theo client_id, SSA không lockout cứng
- 2026-07-05 decision: đăng nhập nhiều bước = state machine password→[đổi MK]→[MFA]→done; trạng thái "đã qua mật khẩu" giữ bằng vé HMAC httpOnly cookie (StageService, TTL 5', ràng uid+userId) — KHÔNG tin FE. Server điều khiển bước kế qua field `next`
- 2026-07-05 decision: MFA enroll qua script server-side scripts/enroll-mfa.js (SSA có quyền server) 2 bước sinh-QR→confirm; UI enroll self-service để E6. MfaService cũng expose API sẵn cho portal
- 2026-07-05 change: sửa 2 bug lúc dựng E2 — (1) KEK mẫu .env.example giải ra 34 byte, AES-256 cần 32 → đổi mẫu (cả guard env.validation + .env); (2) interaction.controller dùng @Res() non-passthrough làm Nest tắt auto-send → helper return object treo vô hạn (đường thành công); fix @Res({passthrough:true}). Bài học: @Res() thô = phải tự res.json, còn muốn return object phải passthrough
- 2026-07-05 constraint: cookie-parser thêm vào main.ts (vé giai đoạn StageService) — đặt trước mount oidc-provider, không ảnh hưởng cookie riêng của provider
- 2026-07-05 event: Epic 3 HOÀN TẤT 6/6 story → KHÉP PHASE 1. E3-S4 /health mở rộng (provider+scheduler heartbeat+stale job, best-effort không treo) + cron heartbeat/phút; E3-S5 CLI rotate-key (add→promote→retire publish-trước-ký-sau + emergency rút kid lộ+audit, verify overlap token cũ vẫn verify — 6/6); E3-S6 archive audit >1năm → .jsonl.gz + xóa bảng nóng (gọi service thật, 5 bản→gz OK); E3-S1 backup trọn bộ (pg_dump+.env+khóa ký, AES-256-CBC pbkdf2, giữ 30, profile compose) + restore drill 21 bảng 0 lỗi; E3-S3 alert.js out-of-band Telegram (healthy/down/scheduler-chết đều bắt); E3-S2 runbook docs/ops/ + diễn tập scaled xong (bare-metal ≤4h còn phải làm)
- 2026-07-05 constraint: rotate khóa cần RESTART sso-server để provider nạp lại jwks (không hot-reload). Dev: docker compose restart (không phải up -d — no-op khi config không đổi). Prod CMD restart cũng nạp lại
- 2026-07-05 constraint: script mới trong apps/sso-server/scripts KHÔNG tự vào container dev — dev mount chỉ overlay src/migrations/scripts/shared; đã thêm mount scripts. Script cũ chạy được vì baked trong image lúc build
- 2026-07-05 decision: backup chạy qua service compose profile 'backup' (postgres:16-alpine có pg_dump, apk add openssl khi thiếu), mount .env+signing_keys ro + backups rw; host cron gọi `compose --profile backup run --rm backup`. Restore: giải mã → tar -x → psql. DB-only KHÔNG đủ (thiếu KEK giải TOTP → SSA kẹt) — bằng chứng vì sao backup trọn bộ
- 2026-07-05 event: code review adversarial vòng 2 (toàn Phase 1, 4 subagent). Boundary bác 3 nghi-Critical (retire rỗng/timingSafeEqual ném/kek blob ngắn — code chặn đúng). 7 fix vòng 1 còn nguyên 100%. Vá 12+ mục "sửa ngay", verify: R11 enroll-mfa dùng dist services (TOTP script enroll app verify OK); R10 vé stage ràng bước (gọi /mfa khi vé=change_password → 401); R4 rate-limit /mfa (429 sau ngưỡng + audit mfa.failed) + window=1; R2 rotate atomic tmp+rename+.bak + jwk.util kid dùng chung + emergency validate; R3 archive chỉ tháng trọn vẹn + cutoff cố định + stream id-batch; R5 token key client_id+IP; R6 backup Dockerfile bake openssl + fail thiếu .env + guard KEEP + verify + chmod600 + iter200k; R1 .catch chống crash + health deadline 2s + cleanup cron oidc_payloads/login_attempts (AD-13) + heartbeat bỏ webhook failed khỏi stale
- 2026-07-05 change: 2 bug thật review vòng 2 — (1) main.ts void rateLimit.record() thiếu .catch = unhandled rejection giết process khi DB blip; (2) rotate-key ghi jwks không atomic → crash = jwks cụt = provider không boot = cả hệ chết. Cả hai code Epic 2/3 mới, test xanh không chạm
- 2026-07-05 constraint: postgres:16-alpine KHÔNG có openssl CLI → backup bake qua deploy/backup/Dockerfile (không apk runtime, offline fail). jwk.util.ts = nguồn duy nhất sinh khóa/kid (KeysService + CLI rotate)

- 2026-07-05 fix: login báo "invalid_request" = oidc SessionNotFound do cookie `_interaction` hết hạn (form mở >10p / tab cũ). Vá: guardInteraction() đổi SessionNotFound→410 interaction_expired; SPA 410→màn "phiên hết hạn"; nâng Interaction TTL 600s→1800s. Verify: 410 khi thiếu cookie, happy-path 201 nguyên vẹn, headless Chrome render form OK.
- 2026-07-05 build E4-S7 (email queue+worker): NotificationsModule = MailerService (nodemailer, host/port từ settings smtp_host/smtp_port, from+creds từ .env, connectionTimeout 10s chống treo) + EmailQueueService.enqueue + EmailWorker (@Interval 15s, claim FOR UPDATE SKIP LOCKED + reclaim 'sending' kẹt >5', retry backoff mũ 60→3600s, MAX 5, throttle BATCH 10). Migration 1720000000004 thêm email_queue.next_attempt_at. Verify E2E qua Mailpit: happy(sent)+fail(pending+backoff, không treo)+retry(attempts=2 delivered). EmailQueueService export cho E4-S3/S5/S6/S8.
- 2026-07-05 QUYẾT ĐỊNH admin-auth (spine để mở): admin xác thực vào API quản trị qua CHÍNH SSO — portal là OIDC client công khai (pmh-portal, PKCE, no secret) → access token là chứng chỉ. AdminGuard verify token OFFLINE bằng node:crypto + khóa công KeysService (không thêm dep, không gọi JWKS mạng), check aud=pmh-portal, rồi tra admin_roles/admin_projects. Thu hồi bám phiên (TTL 5' + refresh trói session, AD-7); trễ ≤5' đã chấp nhận (AD-8). Lý do chọn Bearer thay vì đọc session-cookie: oidc-provider không expose sạch việc đọc session ngoài luồng interaction. UI admin để Epic 6 (phân tách backlog).
- 2026-07-05 build E4-S1/S2 (User CRUD + soft-delete): common/admin/ = AccessTokenService + AdminGuard + @Roles/@CurrentAdmin (@Global AdminAuthModule). UsersModule: create/update (unique email+mã NV tính CẢ deleted → 409 exists_deleted gợi ý reactivate), list/get, softDelete(deleted_at)+reactivate+lock/unlock+revoke-sessions (chỉ SSA @Roles). SessionRevocationService (common, @Global): revokeAllForUser = DELETE oidc_payloads theo accountId (atomic) + mark sessions.revoked_at (FR-05). Event user.deleted/reactivated → user_events. Portal client thêm vào provider.factory seed. Seed-dev thêm SSA break-glass (sysadmin NV000) + project_admin (padmin NV002) + Demo Project. Verify E2E 15/15 (token portal thật qua OIDC headless): 401/403/200 guard, create+unique+400, padmin tạo được nhưng xóa 403, delete+exists_deleted+reactivate, revoke-sessions xóa 18 artifact an.nguyen, user_events+audit đúng.
- 2026-07-05 build E4-S5/S8 (MK tạm + quên MK): TempPasswordService (sinh MK 12 ký tự 4 lớp, set must_change+temp_password_expires_at theo Settings temp_password_ttl_hours, gửi qua email_queue). UsersService.resetPassword (SSA hủy phiên toàn cục / project_admin hủy phạm vi project — SessionRevocation.revokeForUserInProjects theo clients của project). LoginService.validate CHẶN login khi temp_password_expires_at quá hạn (giữ timing đồng nhất). ForgotPasswordController /api/auth/forgot-password: public, backoff theo email (RateLimitService), argon2.hash mồi khi email không tồn tại (chống timing), phản hồi ĐỒNG NHẤT. Verify: temp→login buộc change_password, temp hết hạn→401, forgot real gửi/fake KHÔNG gửi cùng 201, reset SSA gửi email.
- 2026-07-05 build E4-S3/S4/S6: CSV import (csv.util parser RFC4180 tự viết, không dep) — preview soi lỗi từng dòng (trùng DB/trùng file/email sai/group chưa tồn tại+autoCreate), commit PER-ROW (1 dòng hỏng không đổ mẻ), MK tạm qua queue. CSV export scope theo project (SSA tất cả; project_admin qua đồ thị group→client_groups→client→project + allow_all, AD-11). Expiry (E4-S6): migration 1720000000005 thêm users.expiry_warning_sent_at + setting expiry_warning_days=7; ExpiryService @Cron mỗi giờ auto-lock quá hạn (+revoke) & email cảnh báo T-N một lần (cột chống trùng); endpoint set-expiry reset cờ cảnh báo. main.ts nâng body JSON 5mb cho import. Seed thêm client demo-project-app + client_groups(Developers). Verify E2E: preview ok=2/error=4, commit created=2 + 2 email, export SSA 5 rows + padmin CHỈ Developers, cron SQL auto-lock+warning dedup. → EPIC 4 HOÀN TẤT 8/8 story (backend; UI admin ở Epic 6).
- 2026-07-05 CODE REVIEW Epic 4 (thủ công, không git nên không /code-review): 1 HIGH + vá 4, báo 2.
  * HIGH (đã vá+verify): AccessTokenService chấp nhận id_token làm Bearer API quản trị (id_token cùng khóa/iss/aud=pmh-portal, chỉ khác thiếu typ). Thử thật: id_token→200. Vá: bắt buộc header typ='at+jwt' (RFC 9068). Sau vá: access→200, id_token→401.
  * Vá (medium): CSV export formula injection — csvCell chèn ' trước ô mở đầu =+-@\t\r. Verify: full_name '=danger()' xuất ra "'=danger()".
  * Vá (low): UsersService.create() race qua pre-check đụng unique lúc INSERT → giờ bắt 23505 trả 409 (trước 500).
  * Vá (low): CsvImport.createRow — issue() lỗi SAU commit không còn đánh dòng 'failed'/ROLLBACK vô nghĩa; user đã tạo giữ 'created', chỉ log.
  * Vá (low): import cap MAX_ROWS=5000 chống CSV khổng lồ băm argon2 chặn event loop.
  * BÁO (chưa vá): (1) FR-05 "user tự đổi MK → revoke mọi grant TRỪ phiên hiện tại" CHƯA có ở interaction.changePassword (Epic 2) — luồng temp-password/forgot đổi credential nhưng không đuổi phiên cũ; nên vá ở bước change-password (E6 self-service). Không revoke ở forgot (unauth) để tránh griefing. (2) revokeAllForUser seq-scan payload->>'accountId' (chưa index) — ổn ở ~1000 user, cân nhắc functional index nếu phình.
- 2026-07-05 build E5-S4/S1/S7/S2: ProjectsModule (project CRUD + bổ nhiệm project_admin admin_roles+admin_projects, chỉ SSA). GroupsModule (group CRUD — tạo cả 2 vai FR-19, sửa SSA; member add/remove có scope: project_admin chỉ group gán cho client thuộc project mình, gỡ→revoke phạm vi FR-05). BUG BẮT ĐƯỢC + vá: AdminGuard chỉ đọc @Roles ở getHandler → bỏ sót @Roles cấp CLASS (ProjectsController) → padmin tạo được project (leo quyền). Sửa: getAllAndOverride([getHandler, getClass]). Verify: padmin create project 403, member scoping in/out 201/403, appoint/remove OK.
- 2026-07-05 QUYẾT ĐỊNH Path A (client_secret + oidc-provider, mở rộng AD-12): oidc-provider so 1 secret plaintext; AD-11 buộc hash + AD-12 nhiều secret song song → xung khắc. Giải: (1) client DB đọc qua PgAdapter.find('Client') trả metadata + "internal secret" = HMAC(KEK, client_id) (client-secret.util, không lưu đâu, rò DB không lộ); (2) middleware /oidc/token tự verify secret dev với hash active/retiring-còn-hạn (argon2) rồi THAY header Basic bằng internal secret cho provider; secret dev vẫn hash (AD-11), rotate/thu hồi thuần SQL (AD-12). Client tĩnh (demo-app) + public (portal, không Basic) đi thẳng. Disabled → find trả undefined + middleware bỏ qua → token 401.
- 2026-07-05 build E5-S5/S3/S6: ClientsModule — client CRUD (secret hiện 1 lần + hash, disable/enable), rotate ân hạn (client_secret_grace_hours=48, active→retiring), thu hồi ngay secret, client_groups + allow_all (cảnh báo khi tạo group lúc có client allow_all — GroupsService.create). Cron CleanupService dọn secret retiring hết hạn→revoked. Verify E2E PATH A 11/11: authorization_code với secret dev→200, secret sai→401, rotate S2+S1(grace) đều 200, revoke S1→401 S2→200, disable→401. + S3: add/list group, allow_all warning.
- 2026-07-05 GAP CẦN LÀM (security): client_groups CHƯA được enforce làm cổng LOGIN — hiện BẤT KỲ user đăng nhập nào cũng lấy được token cho BẤT KỲ client (loadExistingGrant auto-grant không check membership). AD-11 nói client_groups là "nguồn quyền truy cập" → cần chặn login nếu user không thuộc group của client (trừ allow_all). Điểm enforce: interaction finish() (có params.client_id). Quản lý client_groups đã xong; ENFORCEMENT là việc kế tiếp, security-relevant.
- 2026-07-05 ĐÓNG GAP cổng login (E5-S3 enforcement, AD-11): LoginService.isAllowedForClient(userId, clientId) — cho phép nếu client TĨNH (không trong bảng clients, vd portal/demo-app), HOẶC allow_all_groups, HOẶC user thuộc group đã gán client. interaction.submitLogin chặn NGAY sau xác thực (trước đổi MK/MFA) → 403 "không có quyền truy cập ứng dụng này" + audit login.denied_no_access. SPA hiện 403 inline. Verify: member→201, non-member→403, allow_all→201, portal tĩnh→201 (admin không tự khóa). client_groups giờ thực sự gate login. → EPIC 5 HOÀN TẤT 7/7 + enforcement.
- 2026-07-05 build E6 BACKEND (5 story): S3 audit login.failed(sai MK)+login.success(finish, kèm client_id)+login.denied_no_access. UserGuard (verify token portal, không cần admin, user còn sống) + /api/me: profile+groups, apps (launcher FR-09: client allow_all hoặc user∈group+app_url), sessions (projection), revoke-session, change-password. Settings API SSA (whitelist 14 key + validate int, PUT áp runtime). Audit view scoped (SSA tất/project_admin chỉ project) + archive list/read (giải nén .jsonl.gz, chặn path traversal). FR-05 GIỮ PHIÊN HIỆN TẠI: cookie _session = Session.id (KHÁC uid token tham chiếu) → revokeOtherSessions resolve id→uid rồi hủy phần còn lại; verify: đổi MK từ phiên A→chỉ A sống, B hủy, token A vẫn chạy. Verify curl: /api/me, launcher, settings 200/403/400, audit scoped, FR-05. → E6 backend xong; còn FRONTEND SPA (task còn lại).
- 2026-07-05 build E6 FRONTEND (portal SPA): auth.ts (OIDC Auth Code + PKCE tự viết bằng fetch+crypto.subtle, KHÔNG dep mới; token ở sessionStorage, tự refresh xoay; api() wrapper). App shell: router thủ công (path-based), Sider menu theo VAI (admin thấy Nhật ký, SSA thấy Cấu hình), fetch /api/me (thêm roles/isSsa). 4 trang AntD: Launcher (grid app app_url), SelfService (profile+group+phiên+đổi MK checklist), Settings (SSA sửa tham số runtime), Audit (bảng scoped + chọn lưu trữ tháng). Verify browser THẬT (puppeteer-core + Chrome, headless, screenshot): sysadmin(SSA) menu đủ 4+logout + launcher empty + settings + audit có data; an.nguyen menu 3 mục (ẩn admin) + launcher hiện Demo Project App + account nhóm Developers/Kế toán. 2 BUG FE bắt+vá: (1) React StrictMode gọi effect 2 lần → handleCallback đổi code 2 lần → 400 code consumed → re-login (vá: guard usedCode đồng bộ); (2) race mount2 check isAuthed trước khi mount1 lưu token → login() thừa (vá: boot() memoize module-level, cả 2 mount dùng chung 1 promise). 404 favicon vô hại. → EPIC 6 XONG (BE+FE). PHASE 2 (Epic 4/5/6) HOÀN TẤT.
- 2026-07-05 build EPIC 7 (Directory API + events + webhook) nhánh epic-7-directory-webhook. S1 Directory: AccessTokenService.verifyM2M (client-credentials, bỏ check aud=portal, lấy client_id); DirectoryGuard + /api/v1/{users,users/:id,groups} scope theo client_groups (groups[]=giao user∩client, allow_all KHÔNG nới đọc AD-11), không trả password, pagination MAX 200, rate-limit in-memory 60/phút + audit mọi query, include_deleted. S2 events: UserEventsService.emit (phát user_events + enqueue webhook) wire vào Users(created/locked/unlocked/deleted/reactivated/password)/Groups(groups_changed)/Me(password). GET /api/v1/events?since global tối thiểu, 410 full_resync+cursor khi cursor cũ hơn dữ liệu (dọn 90 ngày ở CleanupService); client resolve chi tiết qua Directory scoped. S3 webhook: migration 06 clients.webhook_url+webhook_secret_enc(KEK); egress.util chống SSRF (https-only, chặn private/metadata/link-local trừ allowlist CIDR, IPv4 CIDR + IPv6 tối giản); ClientsService.setWebhook (validate egress + secret HMAC mã hóa KEK, hiện 1 lần); WebhookWorker (@Interval SKIP LOCKED, re-check egress, HMAC-SHA256 header X-PMH-Signature, timeout 10s, retry backoff MAX 6). Verify: Directory scope 8/8, events 410+cursor+poll, webhook egress block 400 + enqueue + worker retry (192.0.2.1 timeout). BUG bắt+vá: UsersService.emitEvent ghi thẳng user_events → route qua UserEventsService để enqueue webhook. HMAC delivery E2E để Epic 8 demo-app.
- 2026-07-05 build EPIC 8 (docs gate + demo-app). S1 cổng docs: DocsController /api/docs UserGuard + gate group Developers ở BE (non-Developer 403, không lấy được nội dung), nguồn docs/integration/README.md mount vào container dev + COPY vào image prod (Dockerfile). Portal Docs.tsx render markdown tối giản (heading/code/list/bold, không dep), menu "Tài liệu" cho Developers. Verify: an.nguyen(Dev)→200 11442 ký tự, sysadmin→403, browser render OK. S2 demo-app: thêm /directory (client-credentials → Directory API) + POST /webhook verify HMAC-SHA256 TIMING-SAFE (raw body). Verify: chữ ký đúng→200 "webhook OK", sai→401; openssl HMAC khớp công thức worker (đóng gap ký/verify Epic 7). S3 E2E: các mắt xích verify riêng (docs→cấp client→login+verify JWT→Directory→webhook HMAC); link worker→demo-app delivery không chạy live vì egress đòi https+non-private còn demo-app http://localhost (đã note). → EPIC 8 XONG. PHASE 3 (Epic 7+8) HOÀN TẤT. TOÀN BỘ 9 EPIC (0-8) XONG.
